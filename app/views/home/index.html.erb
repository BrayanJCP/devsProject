<div class="row">
    <div class="col-md-8 offset-md-2">
        <ul class="list">
            <% for event in @events %>
               <li>
                   <div class="card">
                       <p><%= event.title%></p>
                       <p><%= event.describe%></p>
                   </div>
               </li>
                <% end %>
        </ul>
    </div>
</div>